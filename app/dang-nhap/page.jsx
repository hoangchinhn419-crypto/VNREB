"use client";import {useState} from "react";import Link from "next/link";import VnrebBrand from "../../components/VnrebBrand";
export default function P(){const[e,se]=useState(""),[p,sp]=useState(""),[m,sm]=useState("");async function sub(x){x.preventDefault();const r=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email:e,password:p})}),d=await r.json();if(!r.ok)return sm(d.error);location.href=["SUPER_ADMIN","ADMIN"].includes(d.user.role)?"/quan-tri":"/tai-khoan"}return <main className="authPage"><section className="authInfo"><Link href="/">← Trang chủ</Link><VnrebBrand/><h1>Chào mừng trở lại VNREB</h1></section><form className="authForm" onSubmit={sub}><h2>Đăng nhập</h2><label>Email<input type="email" value={e} onChange={x=>se(x.target.value)} required/></label><label>Mật khẩu<input type="password" value={p} onChange={x=>sp(x.target.value)} required/></label><button className="primary">Đăng nhập</button>{m&&<p className="authMessage error">{m}</p>}<p>Chưa có tài khoản? <Link href="/dang-ky">Đăng ký</Link></p></form></main>}
