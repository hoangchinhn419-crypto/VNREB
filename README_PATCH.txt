VNREB UI PATCH V7.3 — CHỈ SỬA HERO UI

Chỉ có 4 file cần ghi đè:
1. app/page.jsx
2. app/globals.css
3. components/MotionRuntime.jsx
4. public/brand/v73-city-cinematic.jpg

Không thay backend, database, OpenAI, Dockerfile, package.json, render.yaml.

Cách cập nhật:
- Chép đúng 4 file vào đúng đường dẫn trong repository GitHub hiện tại.
- Commit: Fix VNREB V7.3 cinematic hero motion
- Render > Manual Deploy > Deploy latest commit
- KHÔNG chọn Clear build cache & deploy.

Sau deploy, mở tab ẩn danh hoặc Ctrl + Shift + R.
